<UserControl x:Class="MainProject.MainWorkSpace.Product.ProdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MainProject.MainWorkSpace.Product"
             xmlns:ViewModel="clr-namespace:MainProject.ViewModel"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ViewModel:ProductViewModel}"
             d:DesignHeight="480"
             d:DesignWidth="600">
    <UserControl.Resources>
        <CollectionViewSource x:Key="ClickPro" Source="{Binding AddDetailProToTableCommand}"/>
    </UserControl.Resources>
    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" 
                      materialDesign:ScrollBarAssist.ThumbWidth="2"  >
        <ItemsControl ItemsSource="{Binding ListPoduct}">
            <ItemsControl.Resources>
            </ItemsControl.Resources>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding Source={StaticResource ClickPro}}"
                            Width="Auto" Height="Auto"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Padding="0"
                            Margin="2">
                        <local:Pro_UC DataContext="{Binding}"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"></WrapPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
